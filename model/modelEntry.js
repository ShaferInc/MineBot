const navigation = require('./navigation');
const resourceCollector = require('./resourceCollector');
const farmManager = require('./farmManager');
const builder = require('./builder');
const survivalManager = require('./survivalManager');

module.exports = {
  navigation,
  resourceCollector,
  farmManager,
  builder,
  survivalManager,
  stripMine
};